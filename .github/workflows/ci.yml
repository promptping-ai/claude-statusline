name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:  # No branch filter - allows stacked PRs!

permissions:
  contents: read
  pull-requests: write
  id-token: write  # Required for Claude Code OIDC

jobs:
  ci:
    uses: promptping-ai/.github/.github/workflows/graphite-swift-ci.yml@v1
    secrets: inherit

  rules:
    uses: promptping-ai/.github/.github/workflows/swift-rules-check.yml@v1
    secrets: inherit

  review:
    uses: promptping-ai/.github/.github/workflows/swift-review.yml@v1
    secrets: inherit
